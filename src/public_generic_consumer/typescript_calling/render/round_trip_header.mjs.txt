// Generated execution evidence for the compiled public-generic provider ABI.
// The module owns provider, value, allocation, transfer and cleanup state.
// Run as: node test/round-trip.mjs <path-to-compiled-provider.wasm>

import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import { FIELD_COUNT, MAX_BYTES_PER_LEAF, MAX_TOTAL_PAYLOAD_BYTES } from "../dist/carrier.js";
import { Provider } from "../dist/wasm-provider.js";
import { TRUSTED_BINDING_BYTES, TRUSTED_DESCRIPTOR_BYTES } from "../dist/descriptor.js";
import { SemapraxPublicGenericException } from "../dist/errors.js";

const wasmPath = process.argv[2];
if (!wasmPath) throw new Error("round-trip.mjs requires a compiled provider module path");
const wasmBytes = readFileSync(wasmPath);
