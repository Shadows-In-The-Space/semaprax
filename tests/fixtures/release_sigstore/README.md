# Sigstore verification fixtures

These files are copied from the Apache-2.0-licensed
[`prefix-dev/sigstore-rust`](https://github.com/prefix-dev/sigstore-rust)
repository, crate `sigstore-verify` version `0.11.0`, at Git commit
`ef17cacdbd357befea4c1c768ef02ed9bf52672c`.

| Local file | Exact upstream path |
| --- | --- |
| `cosign-v3-blob.sigstore.json` | `crates/sigstore-verify/test_data/bundles/cosign-v3-blob.sigstore.json` |
| `cosign-v3-blob.txt` | `crates/sigstore-verify/test_data/bundles/cosign-v3-blob.txt` |
| `public-good.json` | `crates/sigstore-verify/test_data/trusted_roots/public-good.json` |

The files are retained solely as third-party interoperability test data under
the upstream Apache License 2.0. They are not Semaprax release artifacts,
signatures, attestations, trusted roots, or evidence that any Semaprax release
has been signed, published, or independently verified.
