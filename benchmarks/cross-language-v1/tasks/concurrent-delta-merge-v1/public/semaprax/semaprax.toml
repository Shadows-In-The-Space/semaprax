schema = "semaprax.project.v1"
name = "concurrent-delta-merge"
entry = "bench.concurrentmerge.app"
sources = ["src/app.spx", "src/candidate.spx", "src/tests.spx"]
web_exports = ["bench.concurrentmerge.merge_concurrent_deltas"]
tests = ["bench.concurrentmerge.tests"]
