schema = "semaprax.manifest.v1"

[package]
name = "std-export-policy"
version = "0.1.0"
profile = "useful-data.v2"

[modules]
entry = "std.export.policy.examples"
sources = ["src/examples.spx", "src/policy.spx", "src/tests.spx"]
tests = ["std.export.policy.tests"]

[exports]
web = []

[dependencies]
std.http = "=0.1.0"
